info:
  name: Resend OTP
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/resend-otp"
  body:
    type: json
    data: |
      {
        "email": "teloja9529@amupx.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
