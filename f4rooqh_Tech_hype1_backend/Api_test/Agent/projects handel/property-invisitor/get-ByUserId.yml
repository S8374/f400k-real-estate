info:
  name: get-ByUserId
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/property-invisitors/user/{{userId}}"
  auth: inherit

runtime:
  variables:
    - name: userId
      value: 315813fe-88b9-40f3-89f9-bbf1e4429df1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
