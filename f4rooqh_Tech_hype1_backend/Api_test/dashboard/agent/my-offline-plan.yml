info:
  name: my-offline-plan
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/payment-plans/my/{{myId}}"
  auth: inherit

runtime:
  variables:
    - name: myId
      value: 55ca7e69-af9f-4a65-a947-72d4dfef1821

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
